@model IEnumerable<ControleDeAcesso.Dominio.Sistema>

@{
    ViewBag.Title = "HomeSistema";
}

<h2> @ViewBag.Titulo </h2>

<p>
    @Html.ActionLink("Novo Sistema", "CadastrarSistema", "Sistema")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idSistema)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idSistema)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Detalhar", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Excluir", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
